---
const { key, namespace = 'site' } = Astro.props;
---

<script is:inline data-astro-rerun data-view-id={`${namespace}:${key}`}>
	(() => {
		const id = document.currentScript.dataset.viewId;

		fetch(`/api/views/${id}`, { method: 'POST' })
			.then(res => res.json());
	})();
</script>
